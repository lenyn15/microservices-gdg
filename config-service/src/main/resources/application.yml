server:
  port: 8081
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/lenyn15/microservices-gdg.git
          searchPaths: config-server
        default-label: master
  security:
    user:
      name: root
      password: s3cr3t
